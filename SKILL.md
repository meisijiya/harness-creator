---
name: harness-creator
description: >-
  构建、审计并改进让 AI 编码代理可靠工作的 harness：AGENTS.md/CLAUDE.md 指令文件、
  验证关卡、范围边界、记忆持久化、上下文预算、工具权限安全以及多代理协调。
  当编码代理跨会话表现不可靠时使用本技能——忘记上下文、偏离范围、在测试通过前就声称"完成"，
  或每次会话的起始状态不一致——或在创建或评估 AGENTS.md、CLAUDE.md、init.sh 时使用。
  状态与交接由已安装的工程 skill 承接，本技能不落这两类产物。
  即使用户从未说出 "harness" 这个词，也应使用本技能。
license: MIT
---

# Harness Creator

让仓库更便于编码代理启动工作、保持在范围内、验证成果并跨会话恢复；harness 保持精简，确保被真正遵循。

不适用于模型选择、孤立的提示词调优、聊天 UI 设计或通用应用架构。

## 核心模型

每个有用的 harness 都包含三个子系统，本技能只负责这三个：

| 子系统 | 最小产物 | 用途 |
|---|---|---|
| 指令 | `AGENTS.md` 或 `CLAUDE.md` | 启动路径、工作规则、完成定义、承接方指向 |
| 验证 | `init.sh` 或已文档化的命令 | 声称完成前必须运行的检查 |
| 范围 | 工单的依赖/阻塞边与完成标准 | 防止越界与半途而废的工作 |

**状态与生命周期不在本技能的子系统里**。上游把两者算进 harness，但这里的答案是**委派**：状态与阻塞边交给 `to-tickets`，会话交接交给 `handoff`。本技能既不落这两类产物，也不为它们指定位置；评估器因此也不给它们打分——否则一个委派正确的仓库会被当成坏仓库。

## 边界：串接，不代做

本技能只负责 harness 产物就位，以及把它们与别的技能串起来；**不承接工程流程本身**。

- **承接方是具名的工程 skill**：需求对齐、规格、拆分、实现、测试、审查、交接由已安装的工程 skill 承担。其中**状态与阻塞边 → `to-tickets`**；**会话交接 → `handoff`**；两者共同的前置是一次性运行 `/setup-matt-pocock-skills` 配置 tracker。本技能**不代为发起、不代为执行**，也不为它们的产物指定位置。
- **不让代理主动扩范围**：不得因本技能而主动开启与 harness 无关的工作——那会与其他 skill 争抢触发时机，导致系统混乱。
- **同一事实只问一次**：属别的 skill 管辖的问题交由它问，本技能不重复询问。
- **只做用户已对齐的事**：用户未陈述的项目事实一律留空待补，不由技术栈推断代填。

## 第一步

1. 检查已有内容：指令文件、验证命令、文档、包清单，以及是否已配置 tracker。→ 输出：产物清单（已有/缺失）。
2. 只询问无法安全推断的缺失上下文：目标代理、期望文件名、对结构的容忍度、是否允许覆盖。→ 输出：确认后的参数。
3. 优先最小化：仅当用户的问题确实涉及跨会话记忆、工具安全、多代理协调或基准测试时，才加载对应参考并加产物。→ 输出：要加载的参考（可为空）
4. 蓝图：把用户**已陈述**的「项目是什么、交付什么」转写成一行，用 `--blueprint` 带入 `AGENTS.md`；未陈述即留「待补」。→ 输出：一行蓝图或「待补」。

## 常见任务

本节的 `<技能目录>` 指技能安装目录（如 `~/.agents/skills/harness-creator/`）。**不要用相对路径**——cwd 在目标仓时必然解析失败（实测退出码 1）。

| # | 任务 | 输入 → 输出 |
|---|---|---|
| 1 | 创建 | 产物清单 + 确认的参数 → `AGENTS.md` + `init.sh` |
| 2 | 审计 | 目标仓路径 → 三子系统得分 + 候选瓶颈 + 前 2-3 项改动 |
| 3 | 报告 | 目标仓路径 + 输出路径 → `--html` 报告（需分享时才做，按需） |

### 创建 harness

```bash
node <技能目录>/scripts/create-harness.mjs --target /path/to/project
```

🔴 CHECKPOINT：先跑 `--dry-run`，把将创建/跳过的产物清单展示给用户，确认后再真正执行。

选项：`--agent-file CLAUDE.md`、`--package-manager npm|pnpm|yarn|bun`、`--commands "cmd one,cmd two"`、`--blueprint "一行蓝图"`；其余见 `--help`。`--force` 覆盖已存在文件——🔴 CHECKPOINT：使用前**必须**获批并列出被覆盖文件；含第三方块的文件一律不用。

**`AGENTS.md` 的章节以 `templates/agents.md` 为唯一来源**；已存在时脚本只报告缺失章节，不写盘，合并由代理执行。

创建后说明创建了什么，并指路承接方：先跑一次 `/setup-matt-pocock-skills`，之后用 `to-tickets` 管状态与阻塞边。本技能**不派生条目、验收标准或决策**。

### 审计现有 harness

```bash
node <技能目录>/scripts/validate-harness.mjs --target /path/to/project
```

报告三子系统得分、最低分区域，以及最先能提升可靠性的 2-3 项改动。最低分只是**候选**瓶颈——先用失败、日志或任务结果确认因果，再声称因果。

### 生成报告

```bash
node <技能目录>/scripts/render-assessment-html.mjs --target /path/to/project
node <技能目录>/scripts/run-benchmark.mjs --target /path/to/project --html /path/to/report.html
```

这是**结构性**基准测试。它先自检——在临时目录搭一套 harness 并验证，证明随附脚本能端到端跑通——再给目标与 eval 覆盖打分。真实有效性仍需在代表任务上做前后对照会话。

## 何时阅读参考文档

仅加载解决当前问题所需的参考文档（位于 `references/`）：

- 跨会话记忆：`memory-persistence-pattern.md`
- 可复用工作流（技能形式）：`skill-runtime-pattern.md`
- 权限、工具、并发：`tool-registry-pattern.md`
- 上下文预算与渐进式披露：`context-engineering-pattern.md`
- 任务委派与并行代理：`multi-agent-pattern.md`
- 钩子、启动、长时间运行的工作：`lifecycle-bootstrap-pattern.md`
- 不易察觉的失败模式：`gotchas.md`

## 异常与边界条件

**绝不静默跳过或静默降级**：一般异常「先告知再执行」，🔴 CHECKPOINT 必须先获批——「我已经能推断出来」不是跳过它的理由。常见的几种：

| 触发条件 | 处理 |
|---|---|
| 目标已有同名文件 | 默认跳过并告知；🔴 批准后才用 `--force`，先列覆盖清单 |
| 目标已有 `CLAUDE.md` | 沿用并编辑它，**不另建 `AGENTS.md`**；除非用户显式要求两个文件 |
| 指令文件已含其他工具的块 | 脚本只报告缺失章节、不写盘；合并由代理执行，保留现有全部内容 |
| 用户要求本技能建仓内状态或交接文件 | 拒绝并指路承接方：`to-tickets`（状态与阻塞边）、`handoff`（会话交接）；**产物位置由承接方决定，本技能不指定**；前置 `/setup-matt-pocock-skills` |
| 无法识别项目栈或包管理器 | 用 `--package-manager` 与 `--commands` 显式指定 |
| 空项目下 `init.sh` **必然失败**（退出码 1） | 这是**正确状态**：占位步骤带 `exit 1`，强制「先替换，再谈完成」。删掉占位行等于门禁消失 |
| `validate` 总分 < 70 | 最低分子系统作为候选瓶颈，给前 2-3 项改动；不堆关键词刷分 |
| 运行环境无 Node | 按 `templates/` 手工创建产物，交付模板内容与手工验证命令 |
| 无法创建文件（权限/只读） | 改为输出确切文件内容与命令，并标注目标路径 |
| `run-benchmark` 自检 FAIL | 视为 skill 自身损坏——先修脚本，再谈交付 |
| 用户问某个承接方装了没 / 装在哪 | **不管**：承接方默认已安装。本技能不检查、不列举、不安装其他 skill，也不为缺失提供替代品——它只管 harness 产物 |

## 设计规则

- 根指令文件保持简短：只做路由与不变量，不做完整手册。判据是「每次动作是否可能需要」，不是「段落有多长」。
- 项目事实放项目文档与目标仓库的指令文件，**不放本技能**。
- 验证命令必须明确且可直接运行。
- 标记工单完成前必须有证据。
- 无显式多代理所有权边界时，一次只推进一个活动工单。
- 状态与交接**不在本仓**：本文件只指向承接方，不建替代品。
- 收尾只作用于**本会话产出**，不做全仓审计；跨会话交接由 `handoff` 按需产生，本技能不主动创建。
- 不得在脚本中隐藏破坏性行为；覆盖必须获得用户显式批准。
- 能写成测试/Schema/门禁的约束一律进 `init.sh`/CI——文字只是指针，不是唯一边界载体。

## 反例黑名单

harness 设计中不要做的事；交付前对照一次。

| # | 反模式 | 为什么不要做 | 替代做法 |
|---|---|---|---|
| 1 | 自建仓内状态/交接文件，充当 `to-tickets`/`handoff` 的替代品 | 与承接方双写必然漂移，代理读到互相矛盾的材料 | 只指向承接方；状态与证据落在其工单，变更交给 git |
| 2 | 无证据标记工单完成 | "声称完成但测试没过"是头号失败 | 证据必填：命令+结果摘要或 CI 链接；无证据不得标完成 |
| 3 | 把项目事实写进本技能，或让技能引用特定项目 | 混入项目事实即腐化 | 项目事实只放目标仓库 |
| 4 | 无多代理所有权边界时同时推进多个工单 | 越界与半途工作的主要来源 | 一次一个活动工单；多代理必须先定义所有权边界 |
| 5 | 为通过 `validate` 审计堆砌关键词 | 审计只看结构化行，堆词无效 | 真实落地 harness；审计只是体检，不是目标 |
| 6 | 静默覆盖已有文件、静默跳过异常 | 破坏用户工作且无法追溯 | 默认跳过+告知；`--force` 走 🔴 CHECKPOINT |
| 7 | 把文字说明当约束的唯一载体 | 代理可能没读到或误解说明 | 能写成测试/Schema/门禁的约束一律进 `init.sh`/CI |
| 8 | 代用户派生条目、验收标准或决策 | 需求对齐不属本技能职责；固化即越权，验收标准会变成代理的想象 | 由 `to-tickets` 从对齐结论派生；本技能只记蓝图概览（`--blueprint`） |
| 9 | 让本技能主动发起工程流程（写工单、开 ADR、拆分任务） | 与其他 skill 冲突、抢触发时机，系统混乱 | 只串接 harness 产物；工程环节交给对应 skill，本技能不代做 |

## 交付清单

最小化 harness 应为目标项目留下：

- [ ] `AGENTS.md` 或 `CLAUDE.md`（含承接方指向：`to-tickets`、`handoff`、`/setup-matt-pocock-skills`）
- [ ] `init.sh`（空项目下**必然失败**：占位验证带 `exit 1`，须替换为真实命令）
- [ ] 已文档化的验证证据或下一步

状态与交接产物**不在交付清单内**：由承接方产生，位置由它们自己决定，本技能不指定。无法创建文件时，改为提供确切的文件内容与命令。
